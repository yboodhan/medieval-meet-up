import React from 'react';


const ViewMatchProfile = props => {
    return (
        <div>Match Profile -- gets the info passed down from clicking the user (hidden)</div>
    )
}

export default ViewMatchProfile