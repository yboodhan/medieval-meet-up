import React from 'react'

import ChatInput from './ChatInput'

const Message = props => {

    // let newMessage;
    // if (chatHistory.length) {
    //     newMessage = props.chatHistory.map((message, i) => {
    //         return (
    //             <div key={i}>
    //                 <p>{message}</p>
    //             </div>
    //         )
    //     })
    // }
    // else {
    //     newMessage = <p>Start your new Chat!</p>
    // }
    
    return (
        <div>
            {/* {newMessage} */}
        </div>
    )
}

export default Message